import React from 'react'

const Contacts = () => {
  return (
    <div>
        <p>pathname: {window.location.pathname}</p>
        <p>pathname: {window.location.search}</p>
    </div>
  )
}

export default Contacts